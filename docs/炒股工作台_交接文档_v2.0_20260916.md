# 炒股工作台（帽子哥板块评分）交接文档 v2.0

> 日期：2026-09-16　|　上一版：v1.0（2026-09-11）
> 站点：**https://chuanwudi46-ops.github.io/Big-A/**　|　仓库：https://github.com/chuanwudi46-ops/Big-A（Public）
> 定位：个人研究工具，**不构成投资建议**
>
> **v2.0 与 v1.0 的关系**：v1.0 是「设计规格书」（当时系统刚跑通、只在合成数据上验证）。
> v2.0 是「现状与交接书」—— 描述**真实上线并已出真数据**的系统，含实测事实、产物契约、
> 踩坑清单与待办。**规格部分（评分维度/权重/决策矩阵）两版一致，v2.0 以实际代码为准做了校准。**
> 阅读建议：接手先看第 1、2 节建立全貌 → 第 4、7 节是最容易踩的坑 → 第 9 节照着做就能维护。

---

## 1. 一页速览

| 项 | 现状 |
|---|---|
| 是否上线 | ✅ 已投产，真实数据每日自动更新 |
| 站点 | https://chuanwudi46-ops.github.io/Big-A/ （手机可直接开，可装 PWA） |
| 板块宇宙 | **申万二级行业 120 个**（2026-09-16 由一级 31 切换） |
| 评分维度 | 12 维（11 正向 + 1 负向罚分），权重外置于 `weights.yaml` |
| 决策输出 | 板块分 S（定方向）× 宏观分 M（定仓位力度） |
| 自选上限 | 12 个（localStorage `wb.sel`） |
| 构建 | GitHub Actions，每交易日 13:40 预热 / 14:00 评分 / 15:30 收盘复核 |
| 单次构建耗时 | **约 1 分钟**（pipeline 约 26 s），窗口余量充足 |
| 服务器 | 无（零运维，仅免费账号） |
| 线上最新 | 120 板块，宏观 65.1「中性」，`intraday=true`，无 demo 标记 |
| 代码规模 | 27 个源文件 **6731 行**（pipeline 3694 / docs 1295 / web 1121 / tools 519 / workflow 102），其中**纯代码 5288 行** |
| 最大待办 | ①真机实测 ②权重校准（需先攒够 2–4 周真实归档快照）③行业估值真实化 |

---

## 2. 现在能做什么（已交付成果）

### 2.1 六个已上线能力

| # | 能力 | 入口 | 说明 |
|---|---|---|---|
| 1 | **板块评分** | 首页「板块评分」标签 | 120 个二级行业按总分排序，含分级标签与动作提示 |
| 2 | **板块详情** | 点板块条 | 评分环 + 11 维横向条形 + 筹码罚分明细 + 该板块新闻流 |
| 3 | **大盘关键位** | 首页常驻卡片（两视图共用） | 上证 / 沪深300 的**周线·月线·年线**压力位与支撑位，含距现价百分比、该高低点日期、贴近标记 |
| 4 | **回撤 / 涨幅** | 「回撤 / 涨幅」标签 | 逐板块的 20/60/250 日**从高点回撤**与**从低点反弹**，含区间位置与距高/低天数；可切窗口、可按 4 种键排序 |
| 5 | **自选板块** | 板块条尾部「＋ 管理」 | 上限 12 个，存 localStorage |
| 6 | **实时补丁 + PWA** | 打开即生效 | 打开时前端直连东财补当日实时快照并在本地微调分；可添加到主屏、离线看上次数据 |

### 2.2 线上实测样本（2026-09-16，供接手时对照）

- **大盘关键位**：上证 3864.28｜周线压力 3899.96(+0.92%)、支撑 3850.86(-0.35%)；
  月线压力 4258.86(+10.21%)、支撑 3794.68(-1.80%)；年线压力 4258.86、支撑 2863.65
- **250 日回撤最深**：航天装备Ⅱ **-53.6%**（距高 165 日）、能源金属 -47.3%、影视院线 -46.3%、
  油服工程 -43.7%、风电设备 -43.5%
- **250 日反弹最强**：玻璃玻纤 **+123.4%**、元件 +119.2%、通信设备 +94.5%、电子化学品Ⅱ +89.2%
- **分级分布**：主升共振 0 / 进入观察 3 / 中性 31 / 转弱 79 / 回避 7

### 2.3 工程成果（v1.0 → v2.0 之间完成）

| 项 | 成果 |
|---|---|
| 投产 | GitHub Pages 上线、Workflow 权限改 `write`、全程可用 REST API 免手工点击 |
| 数据源突围 | 东财 K 线在海外 runner 全不可用 → 换腾讯通道，构建从 **659 s 降到约 26 s** |
| 板块宇宙 | 一级 31 → **二级 120**（含自动剔除 7 个无 K 线的僵尸/缺失标的） |
| 新功能 | 大盘关键位、板块回撤/涨幅 |
| 修掉的静默失效 | ①东财 clist 单页硬截断致实时补丁 2/31 命中 ②分页排序键用涨幅致跨页串位 ③SW 纯缓存优先致发布静默不生效 ④换宇宙后筹码映射不覆盖致罚分算 0 ⑤换宇宙后旧板块文件永不删除 |
| 回归自测 | 新增 `check_render.mjs`（26 项渲染契约）与 `check_live_paging.mjs`（翻页覆盖 + 线上命中率） |
| 手机端 | 二维码 `docs/手机访问二维码.png`、PWA 可安装、safe-area 与 ≥44px 触控适配 |

---

## 3. 系统怎么跑

### 3.1 架构（混合：静态骨架 + 前端实时补丁）

```
GitHub Actions（免费定时执行，无运维）
  13:40 预热   → 抓 120 个板块日K线 / 解禁 / 减持 / 新闻 / 宏观（不依赖收盘）
  14:00 主评分 → 抓盘中快照 → 12 维 → 写 JSON → 提交 → 重新部署 Pages
  15:30 收盘复核 → 用收盘价重算并归档
        ↓ 产物（web/data/**）
GitHub Pages（静态托管，发布的是 CI 上传的 web/ 快照）
        ↓ 手机/桌面打开
纯前端 PWA：读 JSON 秒开 → 1 次 JSONP 补实时 → 本地微调分数 → 刷新 UI
```

**为什么必须混合**：14:00 属盘中，Actions cron 有 5–30 分钟延迟，不能作为盘中决策的唯一时间源；
前端实时补丁保证「此刻打开看到的就是此刻的市场」。

**⚠️ 关键认知**：Pages 发布的是 **CI 上传的 `web/` 快照，不是分支内容**。
改了 `web/` 下的静态文件后，**必须重跑一次 workflow** 才会生效（只推 git 不重跑 = 线上不变）。

### 3.2 每日时序与触发方式

| 时点（北京） | 阶段 | cron (UTC) |
|---|---|---|
| 13:40 | `warmup` 预热 | `40 5 * * 1-5` |
| **14:00** | `score` 主评分 | `0 6 * * 1-5` |
| 15:30 | `close` 收盘复核 | `30 7 * * 1-5` |

手动触发（**不需要点网页**）：
```bash
POST /repos/chuanwudi46-ops/Big-A/actions/workflows/daily.yml/dispatches
body: {"ref":"main","inputs":{"stage":"warmup|score|close"}}
```

**⚠️ 换宇宙或大改后必须「先 warmup 再 score」**：K 线缓存 key 带 `run_id`，
`restore-keys: kline-` 只恢复最近的。换宇宙后旧缓存里没有新板块，
`stage_score` 对无缓存板块会 `[skip]`（结果 count 不足）。只跑 score 会缺板块。

---

## 4. 数据通道：实测事实（本项目最值钱的一节）

### 4.1 东财各域名在海外 runner 的真实可达性（探针实测，非猜测）

| 端点 | 海外 runner | 国内本机 |
|---|---|---|
| `push2.eastmoney.com`（clist/kline） | ❌ 302→502 | ❌ 不可达 |
| `push2his.eastmoney.com`（kline） | ⚠️ 连几次后断连（000） | ❌ 断连 |
| `push2delay.eastmoney.com` **clist** | ✅ 稳定 | ✅ 稳定 |
| `push2delay.eastmoney.com` **kline** | ⚠️ **软限流**：HTTP 200 但 `klines=[]`、`dktotal=0` | ⚠️ 同左 |
| `datacenter-web.eastmoney.com` | ✅ 200 | ✅ 200 |
| 腾讯行情 / 新浪行情 | ✅ 200 | ✅ 200 |

**结论：东财 K 线通道在海外 runner 上彻底不可用，必须换腾讯。**

### 4.2 当前各用途的取数优先级

| 用途 | 优先级 | 备注 |
|---|---|---|
| 板块日 K 线 | **腾讯 → 东财直连 → akshare** | 腾讯唯一可用接口见下 |
| 板块快照 / 成分股 | **东财 `push2delay` clist 直连** | 含 `f62` 主力净流入、`f3` 涨跌幅 |
| 高管持股变动 | **`datacenter-web` + 服务端 filter** | 0.98 s（akshare 同数据要 8 分 49 秒） |
| 前端实时补丁 | 浏览器直连 `push2` → `push2delay` 兜底 | **顺序不能反**（见 4.4） |

### 4.3 腾讯 K 线通道（唯一可用）

```
# 只有这个接口能用（对一级/二级/三级板块都有效）
https://proxy.finance.qq.com/ifzqgtimg/appstock/app/newfqkline/get
    ?param={code},day,{start},{end},{count},qfq

# 代码规则：pt01 + 申万 6 位码
#   一级 电子     = pt01801080
#   二级 种植业   = pt01801012
#   ❌ 不要用 web.ifzq.gtimg.cn/appstock/app/fqkline/get —— 对板块只返回 1 根 bar
```

- **榜单接口只列一级 31 个**：`proxy.finance.qq.com/cgi/cgi-bin/rank/pt/getRank?board_type=hy`
  —— 它只是「发现代码」的入口，**取数要自己用申万码拼**。
  曾误判为「腾讯只支持一级」，实际是榜单的视角限制。
- bar 字段：`[日期, 开, 收, 高, 低, 成交量(手), {}, 换手率%, 成交额(万元), ...]`
  - 成交额 **×1e4** 换成「元」以对齐东财口径
  - **涨跌幅一律用收盘价 `pct_change()` 自算**，不要按 bar 下标取（不同品种长度不一）
  - 取数区间**向前多留 15 天再裁剪**，否则增量更新的首根 bar 涨跌幅为 0
- 实测：**120/120 成功、6.8 s**

### 4.4 东财接口的硬坑（都已踩过，代码里留了注释）

1. **clist 单页 `pz` 被服务端硬截断到 100**：分页终止条件必须拿「服务端实际返回页大小」比，
   不能拿请求的 `pz` 比。否则 496 个板块只取到 100 个。
   - **后端**：板块清单/成分股被静默截断
   - **前端更隐蔽**：`fs=m:90+t:2` 默认按涨幅降序，一次请求 `pz=600` 只回前 100 名 ——
     关心的 31 个板块**只有 2 个命中**，其余 29 个的实时标记与三项微调**静默失效、页面无报错**。
     现按 `data.total` 并发翻满 5 页（`LIVE_PAGE=100`），实测命中 120/120。
2. **分页排序键必须用静态字段 `f12`（代码）+ `po=0`，绝不能用 `f3`（涨幅）**：
   涨幅盘中一直在变，翻页那几百毫秒内列表重排会让跨页边界串位。
   实测 `fid=f3&po=1` 拉 496 个板块会**重复 1 条、漏 1 个**；换 `fid=f12&po=0` 后 496=496 零重复。
   这是历史上「随机掉块」的另一来源（与 K 线通道抖动**是两回事**）。
3. **`datacenter-web` 的 `sortTypes` 个数必须与 `sortColumns` 完全一致**，否则
   HTTP 仍是 200 但 `result: null` + `message: 排序字段和顺序数量不一致`。
   已改为按列数自动生成，且 `result is None` 时**把 message 抛出来**（不抛就无从排查）。
4. **`secids=90.BK1201,...` 在 clist 上不可用**（返回 `rc:102` / `data:null`），
   只能全量拉后本地按 code 建索引。`fs=b:BK1201` 的语义是「该板块的**成分股**」，不是板块自身。

### 4.5 无源标的必须与网络抖动区分开（120 个板块下构建不超时的关键）

| 现象 | 判定 | 处置 |
|---|---|---|
| 每次挂的**是不同标的** | 限流 / 抖动 | 退避重试 + 换源 |
| 固定挂**同一端点** | 地区封锁 | 只能换源 |
| 固定挂**同一批标的** | **无源**，不是抖动 | **立即放弃，不要重试** |

已定义 `NoHistorySource` 异常：空结果或长度不足时抛出 → 业务层**不重试**、返回空 DataFrame、
计入独立计数器 `nosrc`。若给无源标的也配「3 次重试 + 超时」，十几个无源标的
就能凭空多出十几分钟。日志格式 `ok/skip/fail/nosrc` 分开计数，一眼能分辨。

---

## 5. 板块宇宙：申万二级 120 个

### 5.1 是怎么定下来的

```
申万二级官方名单 131 个
   ∩ 东财行业板块清单  → 127 个（4 个东财无同名板块，见下）
   − K 线不可用标的 7 个 → 120 个（当前宇宙）
```

- 生成脚本：`pipeline/build_universe.py`；产物：`pipeline/config/board_universe.json`
- **4 个东财未匹配**：国有大型银行Ⅱ、股份制银行Ⅱ、城商行Ⅱ、农商行Ⅱ
  （东财未做同名板块或层级切分不同）
- **7 个被剔除**（`no_kline`）：林业Ⅱ、农业综合Ⅱ、其他家电Ⅱ、旅游零售Ⅱ、体育Ⅱ、油气开采Ⅱ、医疗美容
  - 6 个腾讯无此指数；1 个（**林业Ⅱ**）是僵尸指数 —— 数据冻结在 2021-12

### 5.2 「可用」的判定判据（两个条件都要，缺一会误判）

```python
usable = len(df) >= 250 and (today - df.index[-1].date()).days <= 45
#        ↑ 条数足够              ↑ 最后一根 bar 足够新
```
只看条数会把僵尸指数误判为可用；它接进宇宙后永远算不出当日动量，把整条链路拖脏。

### 5.3 为什么要保留「可切层级」的能力

`board_universe.json` 的 `level` 字段改一行即可换层级，但它同时带
`levels`（三级名单都有）、`board_codes`、`sw_codes`、`parents`。
**为什么不用东财 496 个**：东财已扩为三级细分共 496 个，且接口不返回层级字段，
无法干净分层。申万官方名单带「上级行业」字段，故以申万为准收敛。

**换宇宙后要跑**：`python pipeline/sync_boards.py --force` 重建元数据，
然后**先 warmup 再 score**（见 3.2）。

---

## 6. 评分体系（与 v1.0 规格一致，此处为代码实际值的校准版）

### 6.1 十二维权重（`pipeline/config/weights.yaml`，改配置不改代码）

| # | 维度 | 权重 | 计算要点 |
|---|---|---|---|
| 1 | 动量 | 10% | 5/20/60 日涨幅加权 + 相对沪深300 超额 |
| 2 | 趋势结构 | 10% | MA20/MA60 位置、多头排列、KDJ(9,3,3) |
| 3 | 量价健康 | 8% | 调整期缩量比、上涨放量比、量价背离 |
| 4 | 换手率位置 | 8% | 板块换手率 250 日分位（低位加分） |
| 5 | 主力资金流 | 10% | 主力净流入占比 |
| 6 | 估值位置 | 10% | PE/PB 历史分位；**当前无真实行业 PE，用 252 日价格分位代理**（产物标 `valuation_source`） |
| 7 | 周期位置 | 8% | 价格动量 + 涨价信号关键词 |
| 8 | 行业出清度 | 6% | 出清链条阶段（**人工维护静态表**） |
| 9 | 政策强度 | 6% | 反内卷 / 供给侧 / 稳增长关键词 |
| 10 | 新闻情绪 | 12% | 情绪分 × 时效衰减 × 板块相关度 |
| 11 | 板块共振 | 12% | 成分股涨跌一致性 + 龙头同步 |
| 12 | 筹码供给 | 罚分 0–20 | 未来 30 日解禁市值/流通市值 + 近期减持公告数 |

### 6.2 计算公式

```
f_i     = 因子按滚动 250 日窗口做分位归一化 → [0,1]
S_base  = 100 × Σ(w_i × f_i)                        # Σw = 1
P_supply = min(20, 12×解禁压力归一 + 4×减持久归一)
命中负面清单 → 分数上限压至 45 并打「回避」
S       = clamp(S_base − P_supply − P_blacklist, 0, 100)
```

### 6.3 分级与动作

| 分数 | 标签 | 动作提示 |
|---|---|---|
| ≥75 | 主升共振 | 逢低吸：网格分批，单次 5% |
| 60–75 | 进入观察 | 小仓试探，跌破支撑才加 |
| 45–60 | 中性 | 卧倒 |
| 30–45 | 转弱 | 逢高减：分批累计 30% |
| <30 | 回避 | 空仓，等解禁出清后的「黄金坑」 |

### 6.4 宏观分 M 与决策矩阵（关键设计，勿改成「乘数」）

宏观四要素：**流动性 30 + 量价 25 + 政策 25 + 情绪 20**，
其中**情绪项做逆向映射**（对应帽子哥「散户情绪极差 ≈ 无关变量甚至反向指标」）。

| 板块分 S ↓ / 宏观分 M → | M≥70 进攻 | 40≤M<70 中性 | M<40 防守 |
|---|---|---|---|
| ≥75 | 按网格分批建仓 | 小仓试探（≤半仓） | 只观察不动手 |
| 60–75 | 观察，跌破支撑再加 | 观察 | 观察 |
| 30–45 | 逢高减 30% | 逢高减 | 逢高减（加速） |
| <30 | 回避 | 回避 | 回避 |

**设计意图**：板块分决定「买什么/卖什么」，宏观分决定「能用多少仓位」。

---

## 7. 产物契约（改前端前必读）

### 7.1 文件清单

| 路径 | 内容 | 更新时机 |
|---|---|---|
| `web/data/meta.json` | 更新时间、`intraday`、宏观分与四要素明细、`count`、`level`、`bench_r20`、`demo` | 每次 score |
| `web/data/index.json` | 板块列表（120 条），供首页排序展示 | 每次 score |
| `web/data/levels.json` | 大盘关键位 | 每次 score |
| `web/data/sectors/<CODE>.json` | 单板块详情（因子、罚分、新闻、swing） | 每次 score |
| `web/data/archive/YYYY/MM/YYYYMMDD_HHMM.json` | 归档快照（回测/复核用） | 每次 score |
| `web/data/*.parquet` | 中间数据缓存（解禁/减持/两融/新闻/指数/社融/交易日历） | warmup |
| `web/data/cache/` | K 线 parquet + 个股-板块映射 + 交易日历（**不入 git**） | warmup |

### 7.2 关键 schema（**字段名容易读错，照抄这里**）

```jsonc
// index.json[N]  —— 注意 swing 是【精简键】
{
  "code": "BK1245", "name": "照明设备Ⅱ",
  "score": 61.7, "label": "进入观察", "action_hint": "观察",
  "parent": "家用电器",                  // 上级一级行业
  "swing": {                            // 键为字符串 "20"/"60"/"250"
    "250": { "dd": -0.2055,             // dd = 从高点回撤（负数）
             "rb":  0.2852,             // rb = 从低点反弹
             "pos": 0.462,              // 区间位置 0=最低 1=最高
             "dsh": 62 }                // dsh = 距高点天数
  }
}

// levels.json
{
  "updated": "...",
  "indices": [{
    "code": "sh000001", "name": "上证指数", "date": "...", "close": 3864.28, "pct": -0.54,
    "periods": [{                       // ← 压力/支撑在 periods[] 里面，不是顶层
      "key": "week", "label": "周线", "bars": 104,
      "range_high": 4258.86, "range_low": 2689.7, "position": 0.749, "position_text": "偏上",
      "resistance": [{ "price": 3899.96, "gap_pct": 0.92, "date": "2025-09-19", "near": false }],
      "support":    [{ "price": 3850.86, "gap_pct": -0.35, "date": "2026-08-28", "near": false }],
      "at_range_high": false, "at_range_low": false
    }                   // week / month / year 三个
    ],
    "summary": { "text": "..." }
  }]                    // sh000001 上证指数 / sh000300 沪深300
}

// sectors/<CODE>.json —— 注意 swing 这里是【完整结构】，与 index.json 不同！
{
  "code": "...", "name": "...", "parent": "...", "score": 62.2, "base": 66.0,
  "label": "...", "action_hint": "...", "blacklisted": false,
  "clearing_stage": "...", "intraday": true, "updated": "...",
  "factors": { "momentum": 0.754, "trend": 0.901, "volume_price": 0.445,
               "turnover": 0.18, "fund_flow": 1.0, "valuation": 0.756, "cycle": 0.0,
               "clearing": 0.8, "policy": 0.667, "news": 0.5, "resonance": 0.652 },
  "penalty": 3.8,
  "penalty_detail": { "release_ratio": ..., "release_mv_yi": ..., "reduction_cnt": ... },
  "valuation_source": "...",
  "swing": { "windows": { "250": { "high": 5723.34, "high_date": "2026-06-22",
                                   "low": 3538.33, "low_date": "2025-10-13",
                                   "drawdown": -0.2054, "rebound": 0.2853,
                                   "position": 0.462,
                                   "days_since_high": 62, "days_since_low": 229 } } },
  "news": [ ... ]
}
```

> **两处 `swing` 结构不同是故意的**：`index.json` 用精简键（避免列表产物膨胀），
> `sectors/*.json` 用完整结构（详情页要高低点日期与距今天数）。
> 排查时**别读错层级** —— 已因此误判过一次「swing 全是 0」。

### 7.3 前端渲染契约

- 两个视图由顶部 `.tabs` 切换，状态在 `state.view`（`score` / `swing`）
- **大盘关键位卡片 `#lvCard` 放在 `#body` 之外** —— 它由 `levels.json` 驱动、
  与视图切换无关，放里面会被评分视图的整块重绘冲掉
- 配色遵循 A 股习惯：**涨/高分用红 `#ef4444`，跌/低分用绿 `#22c55e`**
- 新闻情绪色点：利好红 / 利空绿 / 中性灰

---

## 8. 工程红线与约定（改动前务必确认）

| 约定 | 原因（都是踩过的） |
|---|---|
| **归档必须带 `demo` 字段** | `make_demo.py` 写 `true` → `backtest.load_snapshots` 自动剔除 → 进 `summary.skipped_demo` 头部显示。`main.py` 写 `false`。**新增任何写归档的代码都要带它**，否则合成数据污染 IC / 分层 / 多空胜率 |
| **归一化只做一次** | `chips.release_ratio()` 返回**原始比例**（0.025 = 2.5%），归一化只在 `factors.chip_supply()` 内。曾因双重归一化导致罚分吃满 |
| **构建失败必须保留上一版产物** | `main.py` 在无任何板块完成评分时 `SystemExit` |
| **数据源一律三级重试 + 单点失败降级** | 绝不因单个标的失败中断整体评分 |
| **无源标的不要重试** | 用 `NoHistorySource`，见 4.5 |
| **因子一律输出 [0,1] 分位分（250 日窗口）** | 避免绝对阈值随市场漂移 |
| **接口列名用候选列表匹配** (`_pick_col` / `_pick`) | 抗字段改名；匹配失败返回中性值不报错 |
| **pandas 3.x：`to_numpy()` 可能返回只读视图** | 禁止原地运算（如 `arr -= arr.mean()`），需 `np.array(..., copy=True)` |
| **权重与阈值全部外置到 `weights.yaml`** | 改配置不改代码 |
| **K 线缓存绝不入 git** | parquet 每日全量重写，约 3 MB/日 → 年约 1 GB。CI 用 `actions/cache@v4` |
| **改前端后必须升 `web/sw.js` 的 `VERSION`** | 浏览器只在 `sw.js` **字节变化**时才重装 SW。只改 `app.js` 不改 `sw.js` → 老用户永远看到旧界面且**毫无报错**。当前 `wb-v2` |
| **改 `web/` 下静态文件后必须重跑 workflow** | Pages 发布的是 CI 上传的快照，不是分支内容 |

### 8.1 Service Worker 缓存策略（已从 cache-first 改为 SWR）

静态资源用 **stale-while-revalidate**：先返回缓存秒开，后台带 `cache:'no-cache'`
条件请求刷新（GitHub Pages 有 ETag，未变更仅 304），命中 200 才写回。
这样**下次加载自动拿到新版**，不再只依赖「记得改 VERSION」这种人工纪律。

`data/` 走**网络优先**（保证评分是当天最新的），失败回退缓存（离线可看）。

---

## 9. 日常运维手册

### 9.1 环境准备

| 项 | 值 |
|---|---|
| Python | 3.13，虚拟环境 `C:\Users\40818\.workbuddy\binaries\python\envs\default` |
| Node | 22.22.2，`C:\Users\40818\.workbuddy\binaries\node\versions\22.22.2-3\node.exe` |
| Git | **本机无独立 Git**，用 WorkBuddy 自带 PortableGit 1.2.0：`C:\Users\40818\.workbuddy\binaries\PortableGit\versions\1.2.0` |
| 沙箱注意 | bash 环境可能丢 `PATH`（`ls`/`git`/`head`/`dirname` 都找不到）。**用绝对路径调 Python，避免管道**；需要 git 时显式把 PortableGit 的 `cmd` 与 `mingw64\bin` 加进 `PATH` |

### 9.2 常用操作

```bash
PY=C:/Users/40818/.workbuddy/binaries/python/envs/default/Scripts/python.exe
NODE=C:/Users/40818/.workbuddy/binaries/node/versions/22.22.2-3/node.exe

# 本地跑管道（main.py 支持 --stage / --force / --rebuild-map）
$PY pipeline/main.py --stage warmup      # 抓 K 线/解禁/新闻（120 个板块约 7 s）
$PY pipeline/main.py --stage score       # 评分 + 产出 web/data（约 9 s）
$PY pipeline/main.py --stage close       # 收盘复核归档
$PY pipeline/main.py --stage score --force          # 忽略交易日判断
$PY pipeline/main.py --stage warmup --rebuild-map   # 强制重建个股-板块映射

# 板块宇宙（--level 可切层级，--report 只打印不写文件）
$PY pipeline/build_universe.py --report
$PY pipeline/build_universe.py --level 申万一级行业
$PY pipeline/sync_boards.py --force       # 换层级后必须重建元数据

# 回归自测（改前端必跑）
$NODE tools/check_render.mjs             # 26 项渲染契约
$NODE tools/check_live_paging.mjs        # 翻页覆盖 + 线上命中率

# 离线演示数据（无网环境验证全链路）
$PY pipeline/make_demo.py --simulate 60

# 回测（--horizons / --quantiles / --min-boards / --archive / --cache / --out / --csv）
# 产出 docs/backtest_report.md + web/data/backtest_detail.csv
# 当前只积累了 1 份归档快照，脚本会提示「样本不足，仅输出说明性报告」——
# 这是预期行为，不是 bug。攒够 2–4 周快照后才有统计意义。
$PY pipeline/backtest.py
$PY pipeline/backtest.py --horizons 3,10,30 --quantiles 5

# 重新生成手机二维码（位置参数：url、输出路径）
$PY tools/make_qr.py [url] [out]
```

### 9.3 发布流程（`git push` 不通时的通道）

本机到 `github.com:443` 会被中断，**改用 `tools/api_push.py` 走 Git Data API**
（只经 `api.github.com`，不依赖本地已有远端历史，也不覆盖 CI 写入的数据产物）。

```bash
# 预览（默认不加 --apply 只打印将要改什么）
$PY tools/api_push.py --only web/app.js

# 实际提交
$PY tools/api_push.py -m "fix: 修正 xx" --only web/app.js --only pipeline/main.py --apply

# 删除远端文件
$PY tools/api_push.py --delete .github/workflows/probe.yml --apply
```

**⚠️ 提交信息必须用 `-m` 显式给**：原实现取 `git log -1`，
而本地 HEAD 长期停在很早的提交，会让每次推送都挂着同一条与改动无关的旧消息
（历史里 `8188ea2` / `67d5867` 就是这么来的，已无法追溯）。

若日后 `git push` 恢复可用，记住它的坑：PortableGit 的 system config 设了
`credential.helper=helper-selector`，非交互场景无法弹窗，
直接 push 报 `could not read Username`。解法是显式指定 GCM：
```bash
git -c credential.helper=manager push
```

### 9.4 发布检查清单（每次改完前端照做）

- [ ] `$NODE tools/check_render.mjs` 全绿（26/26）
- [ ] `$NODE tools/check_live_paging.mjs` 全绿（线上命中 120/120）
- [ ] 改了静态资源 → **已升 `web/sw.js` 的 `VERSION`**
- [ ] 已 `api_push --apply` 推送
- [ ] 已 dispatch workflow（warmup + score）**并等到 success**
- [ ] 线上逐字节核对：`app.js` / `index.html` / `styles.css` / `sw.js` / `manifest.json` 与本地 md5 一致
- [ ] 线上 `meta.json`：`count=120`、`level="申万二级行业"`、**无 `demo` 字段**

---

## 10. 代码地图

| 文件 | 行数 | 职责 |
|---|---|---|
| `pipeline/sources.py` | 810 | **数据源适配层**（K线走腾讯；快照/成分股走东财直连；akshare 兜底），每函数自带重试与降级 |
| `pipeline/main.py` | 461 | 管道入口 `warmup` / `score` / `close`；并发取数；清理已移出宇宙的产物 |
| `pipeline/backtest.py` | 350 | IC / ICIR / 分层 / 多空胜率 / 多空净值 |
| `pipeline/make_demo.py` | 319 | 离线合成数据（`--simulate N`） |
| `pipeline/macro.py` | 306 | 宏观四要素 + 交易日历（**情绪项返回"温度"，逆向由 `score.macro_score` 统一处理**） |
| `pipeline/levels.py` | 289 | 大盘关键位：周线 104 / 月线 60 / 年线 10 根，摆动高低点定压力/支撑 |
| `pipeline/chips.py` | 238 | 解禁/减持按「个股 ∩ 板块成分股」精确归属；**换宇宙后自动校验映射覆盖率** |
| `pipeline/sync_boards.py` | 232 | 生成 `sector_meta.json`（含二级继承一级的 `clearing` / `blacklist`） |
| `pipeline/factors.py` | 221 | 11 个正向因子 + 筹码罚分，全部输出 [0,1] |
| `pipeline/build_universe.py` | 219 | 由申万名单 ∩ 东财生成 `board_universe.json`，按 K 线可用性自动剔标 |
| `pipeline/swing.py` | 134 | 20/60/250 日窗口的回撤与反弹；`brief()` 出列表精简版 |
| `pipeline/score.py` | 69 | 加权聚合 + 宏观矩阵 + 分级 |
| `pipeline/config/weights.yaml` | 46 | **全部权重与阈值**（改这里不改代码） |
| `web/app.js` | 682 | 前端全部逻辑：渲染、视图切换、实时补丁、IndexedDB 缓存 |
| `web/styles.css` | 304 | 移动端样式（暗色、safe-area、≥44px 触控） |
| `web/sw.js` | 82 | Service Worker（`data/` 网络优先；静态资源 SWR） |
| `web/index.html` | 53 | 页面骨架（`#lvCard` 在 `#body` 之外） |
| `tools/check_render.mjs` | 141 | 渲染契约自测（26 项） |
| `tools/check_live_paging.mjs` | 95 | 前端实时取数自测（翻页覆盖 + 命中率） |
| `tools/api_push.py` | 191 | Git Data API 推送（`--only` / `--delete` / `-m` / `--apply`） |
| `tools/make_qr.py` | 92 | 生成手机访问二维码 |
| `.github/workflows/daily.yml` | 102 | 三阶段调度 + K 线缓存 + 提交产物 + 部署 Pages |

---

## 11. 回归测试（改前端必跑，这是唯一的防线）

浏览器里跑的逻辑 **CI 和本机都覆盖不到**，一旦产物字段改名，
页面上只会**静默地少显示一块内容**（甚至白屏），没有任何报错。故有两个 node vm 自测：

| 脚本 | 覆盖 | 断言数 |
|---|---|---|
| `tools/check_render.mjs` | 大盘关键位卡片、回撤列表行数与位置条、窗口×排序 12 种组合无 NaN、详情页因子拆解、产物 schema 完整性 | **26** |
| `tools/check_live_paging.mjs` | 域名优先级、单页上限、翻页取全量、零重复、**线上真实清单命中率** | 6 |

做法：把 `web/app.js` 丢进 node vm、禁用 `boot()`、塞最小 DOM stub，
再用**线上/本地真实产物**调用渲染函数，断言输出 HTML 里确实出现关键内容。

**验收标准**：命中率必须 **100%**，且「拉回条数 == 唯一 code 数」。
`check_live_paging.mjs` 同时也是「翻页有没有真的生效」的哨兵 ——
不翻页只有 68/120，一跑就知道。

---

## 12. 已知问题与待办（按优先级）

### P0 —— 建议接手第一件事

| 项 | 说明 |
|---|---|
| **真机实测** | 本机无法模拟手机网络。需在 iOS Safari / Android Chrome 各开一次，确认首屏、实时补丁、PWA 安装、自选 12 个上限的实际表现 |

### P1 —— 影响结论可信度

| 项 | 说明 |
|---|---|
| **权重校准** | 当前 12 维权重是**先验值**。需先积累 **2–4 周真实归档快照**（现仅 9 份，远不够），再用 `backtest.py` 迭代。**这是让系统从「能算」变成「算得准」的关键一步**。<br>⚠️ 刚跑过一次：`[backtest] 快照 1 个，K线 120 个板块` → 「样本不足，仅输出说明性报告」。<br>注：本地只有 1 份归档，远端已有 9 份 —— **校准前先把 CI 归档拉回本地** |
| **行业估值真实化** | 估值维度（权重 10%）现在用 252 日价格分位**代理**。<br>**tushare 这条路已确认走不通**：`index_dailybasic` 需 4000 积分起，且官方文档明确只提供上证综指/深证成指/上证50/中证500/中小板指/创业板指，**不含任何行业**，充值也拿不到。<br>**下一步**：走 akshare 免费行业市盈率（巨潮 / 中证），产物里的 `valuation_source` 字段就是为这个预留的 |
| **行业出清度仍是人工表** | 权重 6%，含主观性。页面已标注，暂不参与短期评分主导 |

### P2 —— 增强项

| 项 | 说明 |
|---|---|
| 板块成分股时效 | 映射缓存 7 天，成分股调整期内可能短暂不准 |
| 回撤/涨幅的历史分位 | 现在是绝对值，可加「当前回撤在自身历史中的分位」增强可比性 |
| Capacitor 打包 APK | 若需要真正的原生 App |
| 三级行业支持 | `board_universe.json` 已带三级名单（335 个），改 `level` 字段可切换，但需评估 335 个标的的构建耗时与腾讯 K 线覆盖率 |

### 已知的技术债（不影响使用，但接手要知道）

| 项 | 说明 |
|---|---|
| 历史提交消息错乱 | `8188ea2` / `67d5867` 两条消息是错的（`api_push` 缺陷所致），已在提交信息里说明，**不建议改写历史** |
| 本地 git 与远端不同步 | 长期走 `api_push.py` 推送，本地 HEAD 停在 `e01c0b5`。若日后要用本地 git，**先 fetch 再对齐**，别直接 push |
| 本地 `web/data` 落后于线上 | `api_push` 默认跳过 `web/data`（避免覆盖 CI 产物），本地归档只有 1 份而远端有 9 份，属预期现象 |

---

## 13. 踩坑清单（按「静默程度」排序，送给你）

**静默失败最可怕 —— 页面看起来完全正常，结论已经错了。**以下 8 条都是实际踩过的：

| # | 坑 | 表现 | 教训 |
|---|---|---|---|
| 1 | 东财 clist 单页硬截断到 100 | 后端只取到 100 个；前端 29/31 个板块的实时标记**无报错地失效** | **分页终止条件拿「服务端实际页大小」比，不是请求的 `pz`** |
| 2 | 分页排序键用 `f3`（涨幅） | 496 条拉回但只有 495 个唯一，随机掉块 | **翻页只用静态字段排序（代码 `f12`）** |
| 3 | SW 纯缓存优先 + `sw.js` 未变 | 改动推上去了但老用户永远看到旧界面 | **改前端必升 `VERSION`**；已改 SWR 作第二道保险 |
| 4 | 换宇宙后筹码映射不覆盖新标的 | 筹码罚分静默算成 0，页面无异常 | **持久化派生映射要按宇宙校验覆盖率** |
| 5 | 换宇宙后旧板块 JSON 永不删除 | 切换后 **151 个文件而宇宙只有 120 个**（多出 31 个旧一级板块），且**能被匿名访问**（旧数据当当前数据发布） | **按当前宇宙清理产物目录**（用宇宙而非「本轮成功项」做基准） |
| 6 | 腾讯「行业榜单」只列一级 | 误判为「换源只支持一级」差点放弃方案 | **榜单是发现代码的入口，取数要自己拼码** |
| 7 | 僵尸指数（林业Ⅱ 数据冻结在 2021-12） | 只看条数会误判为可用，接进宇宙后算不出动量 | **可用性判据要同时看「条数」和「最新一根 bar 的日期」** |
| 8 | 双重归一化 | 筹码罚分吃满 | **归一化只做一次**，原始比例与归一化分层放 |

另有一条不是静默、但很费时的：**akshare 的 `stock_hold_management_detail_em` 无日期过滤、
翻全部 344 页 = 8 分 49 秒**。改为直连 `datacenter-web` + 服务端 `filter` 后 **0.98 秒**，
并做了同时点严格等价验证（近 30 日减持记录 479=479、涉及股票 139=139、集合差异为空）。
**能用服务端 filter 就不要本地筛选。**

---

## 14. 免责声明

本系统为**个人研究工具**，基于公开免费数据与帽子哥公开内容的**结构化提炼**。
评分不代表事实、**不构成投资建议**，实际决策风险自负。
数据源为第三方免费公开接口，可用性不保证；构建失败时保留上一版产物。
