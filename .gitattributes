# 统一换行符为 LF，避免 Windows 本地与 Linux(CI) 之间的 CRLF 差异
* text=auto eol=lf

# 二进制文件不做换行转换
*.parquet binary
*.png     binary
*.jpg     binary
*.jpeg    binary
*.ico     binary
*.woff    binary
*.woff2   binary
